'use strict';

// Define the `heatmapGenerator` module
angular
  .module('heatmapGenerator', [
    'core.heatmap'
  ]);

// Define the `exposureChart` module
angular
  .module('heatmapChart', [
    'core.heatmap'
  ]);
